app:
  telegram-token: ${TELEGRAM_TOKEN} # env variable

spring:
  application:
    name: Bot
  liquibase:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

scrapper:
  url: http://localhost:8081

server:
  port: 8080

springdoc:
  swagger-ui:
    enabled: true
    path: /swagger-ui
